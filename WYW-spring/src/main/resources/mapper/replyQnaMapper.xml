<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.project.WYW.mapper.replyQnaMapper">
    <insert id="regReplyQna" parameterType="ReplyQnaVo">

        INSERT INTO product_qna (product_id, user_id, content)
        VALUES (#{productId}, #{userId}, #{content});

    </insert>

    <select id="getReplyQnaList" resultType="ReplyQnaVo">
        SELECT qna_id   AS qnaId,
               product_id AS productId,
               user_id    AS userId,
               created_at AS createdAt,
               content,
               is_answered AS isAnswered
        FROM product_qna
        WHERE product_id =
              #{productId}
        ORDER BY created_at DESC
            limit #{skip}, #{amount}
    </select>

    <select id="getReplyQnaTotal" resultType="int">
        SELECT count(*)
        FROM product_qna
        WHERE product_id =
              #{productId}
    </select>

    <select id="getUpdateReplyQna" resultType="ReplyQnaVo">
        SELECT qna_id   AS qnaId,
               product_id AS productId,
               user_id    AS userId,
               created_at AS createdAt,
               content,
               is_answered AS isAnswered
        FROM product_qna
        WHERE qna_id =
              #{qnaId}
    </select>

    <update id="updateReplyQna" parameterType="ReplyQnaVo">

        UPDATE product_qna SET content = #{content}
        WHERE qna_id = #{qnaId}

    </update>

    <delete id="deleteReplyQna" parameterType="ReplyQnaVo">

        DELETE FROM product_qna
        WHERE qna_id = #{qnaId}

    </delete>
</mapper>